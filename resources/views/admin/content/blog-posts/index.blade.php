@extends('layouts.admin')
@section('content')
<div class="flex items-center justify-between mb-4"><h2 class="text-2xl font-semibold">Blog Posts</h2><a href="{{ route('admin.blog-posts.create') }}" class="px-4 py-2 bg-slate-900 text-white rounded">Add post</a></div>
<form method="GET" class="mb-4 grid md:grid-cols-4 gap-3"><input name="search" value="{{ request('search') }}" placeholder="Search" class="border rounded px-3 py-2"><select name="category_id" class="border rounded px-3 py-2"><option value="">All categories</option>@foreach($categories as $category)<option value="{{ $category->id }}" @selected((string)request('category_id')===(string)$category->id)>{{ $category->name['en'] ?? $category->slug }}</option>@endforeach</select><select name="published" class="border rounded px-3 py-2"><option value="">Any status</option><option value="1" @selected(request('published')==='1')>Published</option><option value="0" @selected(request('published')==='0')>Draft</option></select><button class="px-3 py-2 border rounded">Filter</button></form>
<div class="bg-white border rounded overflow-x-auto"><table class="min-w-full text-sm"><thead class="bg-slate-50"><tr><th class="p-3 text-left">Title</th><th class="p-3 text-left">Category</th><th class="p-3 text-left">Publish date</th><th class="p-3">Status</th><th class="p-3"></th></tr></thead><tbody>@forelse($blogPosts as $post)<tr class="border-t"><td class="p-3">{{ $post->title['en'] ?? $post->slug }}</td><td class="p-3">{{ $post->category?->name['en'] ?? '-' }}</td><td class="p-3">{{ optional($post->published_at)->format('Y-m-d H:i') ?? '-' }}</td><td class="p-3 text-center">{{ $post->is_published ? 'Published':'Draft' }}</td><td class="p-3 text-right"><a class="text-blue-600" href="{{ route('admin.blog-posts.edit',$post) }}">Edit</a><form method="POST" action="{{ route('admin.blog-posts.destroy',$post) }}" class="inline">@csrf @method('DELETE')<button class="text-red-600 ml-3">Delete</button></form></td></tr>@empty<tr><td class="p-3" colspan="5">No posts found.</td></tr>@endforelse</tbody></table></div>
<div class="mt-4">{{ $blogPosts->links() }}</div>
@endsection
