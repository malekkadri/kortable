@extends('layouts.admin')
@section('content')
<div class="flex items-center justify-between mb-4"><h2 class="text-2xl font-semibold">Blog Categories</h2><a href="{{ route('admin.blog-categories.create') }}" class="px-4 py-2 bg-slate-900 text-white rounded">Add category</a></div>
<form method="GET" class="mb-4 grid md:grid-cols-3 gap-3"><input name="search" value="{{ request('search') }}" placeholder="Search" class="border rounded px-3 py-2"><select name="active" class="border rounded px-3 py-2"><option value="">All</option><option value="1" @selected(request('active')==='1')>Active</option><option value="0" @selected(request('active')==='0')>Inactive</option></select><button class="px-3 py-2 border rounded">Filter</button></form>
<div class="bg-white border rounded overflow-x-auto"><table class="min-w-full text-sm"><thead class="bg-slate-50"><tr><th class="p-3 text-left">Name</th><th class="p-3 text-left">Slug</th><th class="p-3">Status</th><th class="p-3"></th></tr></thead><tbody>@forelse($blogCategories as $category)<tr class="border-t"><td class="p-3">{{ $category->name['en'] ?? $category->slug }}</td><td class="p-3">{{ $category->slug }}</td><td class="p-3 text-center">{{ $category->is_active ? 'Active':'Inactive' }}</td><td class="p-3 text-right"><a class="text-blue-600" href="{{ route('admin.blog-categories.edit',$category) }}">Edit</a><form method="POST" action="{{ route('admin.blog-categories.destroy',$category) }}" class="inline">@csrf @method('DELETE')<button class="text-red-600 ml-3">Delete</button></form></td></tr>@empty<tr><td class="p-3" colspan="4">No categories found.</td></tr>@endforelse</tbody></table></div>
<div class="mt-4">{{ $blogCategories->links() }}</div>
@endsection
